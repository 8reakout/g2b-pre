name: G2B Prespec Email Notice

on:
  workflow_dispatch:
  schedule:
    # 매주 월요일 11:07 KST = 02:07 UTC
    - cron: "57 23 * * 2"

permissions:
  contents: write

jobs:
  run-monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run G2B prespec email monitor
        env:
          G2B_PRESPEC_SERVICE_KEY: ${{ secrets.G2B_PRESPEC_SERVICE_KEY }}
          G2B_SERVICE_KEY: ${{ secrets.G2B_SERVICE_KEY }}
          DATA_GO_KR_SERVICE_KEY: ${{ secrets.DATA_GO_KR_SERVICE_KEY }}
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USE_TLS: ${{ secrets.SMTP_USE_TLS }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          MAIL_FROM: ${{ secrets.MAIL_FROM }}
          MAIL_TO: ${{ secrets.MAIL_TO }}
          MAIL_CC: ${{ secrets.MAIL_CC }}
        run: |
          python main.py

      - name: Commit updated prespec state
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add seen_prespec_ids.json latest_prespecs.json || true
          git commit -m "Update G2B prespec notices" || echo "No changes to commit"
          git push
